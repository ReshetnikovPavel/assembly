as hello.s -o hello.o
ld hello.o -o hello
./hello


mov $1, %rax - хотим сисколл write
mov $1, %rdi - хотим в stdout
greet - это метка (указатель). На самом деле просто чиселка

$ - мы работаем с константой
"По адресу" пишется без $
mov $13, %rdx - сообщ длину

man syscall - за инфой о регистрах

int write(int handle, void *buf, int count)

Существует таблица вызовов С в линуксе
1 - write
60 - exit
Поэтому в rax записываем 60

Таблица сисколлов лежит в /usr/include/asm/unistd_64.h

Наш сисколл вызвался, когда написан syscall

mov Размер значения записывается по размеру регистра
movq Размер 64 бита

h/w:
    понять hello_world
    вызвать syscall read, передав некоторый буффер
    Программа, которая запрашивает имя и выводит его в виде "Hello NAME!\n"
    ($greet+6)

